spring:
  application:
    name: userserver #服务名称
  profiles:
    active: dev #环境`
  cloud:
    nacos:
      server-addr: localhost:80 #nacos地址  读取nacos配置  换成80就是集群了
      config:
        file-extension: yaml  #文件后缀名